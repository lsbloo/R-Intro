
# Criar graficos
plot(1:30)
hist(rnorm(10))

#Instalar pacotes
install.packpages('randomForest')


# Carregar Pacote
library(randomForest)
#Descarregar pacote
detach(packpage:ggplot2)

# nome da funcao se souber
help("mean")
# se nao souber
help.search('matplot')

# exemplos como utilizar algum item
example('matplot')
